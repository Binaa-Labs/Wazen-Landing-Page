/* Confirmed app URLs (July 2026). Launch checklist: click-test both return
   200 once the domain is verified live. */
export const APP_URLS = {
  signup: "https://app.wazen.fit/signup",
  login: "https://app.wazen.fit/login",
} as const;
