/* Feature flags — deliberately tiny. Each flag is a one-line edit to flip. */

/* Video tour section (D20): ships VISIBLE in placeholder mode (dummy asset,
   real chapter structure) while the real walkthrough video is in production.
   Set to false to hide the whole section at merge time. */
export const SHOW_VIDEO_SECTION = true;
