# Wazen Landing Page — Project Document

> Single source of truth for this repo. A developer joining cold should get the full picture from this file: what Wazen is, what this repo is, every decision made and why, what's shipped, and what remains.
> Current state: **Phase 1 complete on `feat/landing-revamp`** (Pass A ✅ · A.1 ✅ · B ✅ · C.1 ✅ · C.2 ✅ · SEO baseline ✅, D16). **Phase 2 on `feat/phase-2-design`: design proposal ✅ · Pass 2.1 ✅ (hero Option A · video placeholder · product-proof · nav + real logo · TrustBar strip — D17–D25).** **Follow-up fix ✅ (OG/social card layout EN+AR · WhyWazen mobile overflow).** **Stage-2 section-redesign proposal ✅ (PHASE2-STAGE2-DESIGN.md; all 14 checklist answers recorded as D26–D39) · Pass 2.2a ✅ (Problem chaos→calm split · Features rail tabs + composed scene) · Pass 2.2a.1 ✅ (tabs 2/3 coach-view capture swaps · USD default both locales — D40/D41) · Pass 2.2b ✅ (HowItWorks zigzag timeline · Segments persona mosaic + R1 measured AA scrims · photo divider ① — D42) · Pass 2.2c ✅ (ClientApp cinematic dark band + white-label phone · WhyWazen editorial collage · CTA OG-card recomposition · the four queued i18n changes incl. the tab-2 `primaryAlt` fix — D43) · Pass 2.2c.1 ✅ (owner device-review fix-ups: WhyWazen mobile Option A overlap stack · CTA mobile bleed removed — D44/D45) · Pass 2.3a ✅ (photo integration: per-persona hero photos H-1/2/3 · divider D-1 · ClientApp D-2, R3 closed — D46–D48) · Pass 2.3b ✅ (white-label capture shipped · hero mobile crop · video shipped hidden — D49–D51) · Pass 2.4 ✅ (pricing truth alignment per Billing_Final.md + owner rulings · tab title/icon light-mode · D43 orphans retired · repo hygiene — D52). **Phase 2 SHIPPED — live on `main` (merge `09dfaee`,** July 2026: owner-approved direct `--no-ff` merge of `feat/phase-2-design` into `main`; both feature branches retained — see §2 Deployment**).** Remaining: three open launch-checklist items (§6: CTA click-test · WhatsApp OG re-verify · hosted rich-results validation) + the post-launch backlog (§6: video enable · founder portrait F-11 · re-capture ledger session · photo set v2 · dead-code cleanup · Phase-7 copy reminders).

---

## 1 · Product context

**Wazen (وازن)** is a B2B2C coaching platform for the Gulf/MENA market: a **coach dashboard** (web) plus a **client app** where practitioners manage clients, workout/nutrition/supplement/medication plans, structured check-ins, progress tracking, and messaging.

- **Who it's for:** fitness coaches, nutrition coaches, and health/wellness practitioners. Clients never sign up publicly — they **join by coach invite only**. The platform is sold to the practitioner; the client app is included in the practitioner's plan at no cost to clients.
- **Regional identity:** built Arabic-first — full RTL in both the product and this landing page, not a bolted-on translation. Primary markets: UAE, GCC, wider MENA.
- **The client app is a PWA.** It works on any phone via the browser or saved to the home screen — nothing to download from an app store. Native apps are planned later; until then, marketing copy must never claim a native "mobile app" (see D8).
- **Business model:** SaaS subscription. Free forever for the first 5 active clients (no trial timer, no credit card), then Professional ($49/mo) and Premium ($99/mo) tiers with client-capacity add-ons.
- **Parent company:** **Binaa Labs** is the display name; **Binaa Lab** is the legal name and appears **only** in the Terms/Privacy legal pages. On the landing page, Binaa Labs appears in exactly two places (footer copyright + FAQ Q6) — see D2.

**This repo is the marketing landing page (wazen.fit) only** — not the app. The app lives at **app.wazen.fit** (signup/login URLs in `lib/links.ts`; live click-test pending on the launch checklist).

---

## 2 · Tech & conventions

**Stack:** Next.js 16 (App Router, Turbopack) · TypeScript · Tailwind CSS v4 (CSS-first config via `@theme` in `app/globals.css`) · Framer Motion. No UI component libraries (no Shadcn/Radix/Headless) — every primitive is hand-built in `components/ui/`.

**Read `node_modules/next/dist/docs/` before writing code** (AGENTS.md rule) — this Next.js version has breaking changes vs. training data. Known example: `priority` on `next/image` is deprecated in favor of `preload`.

Single-source-of-truth files — change these, not call sites:

| File | Owns |
|---|---|
| `lib/i18n.ts` | Every user-facing string, EN + AR. `type Dictionary = typeof en` forces the AR object to match shape at compile time. Structural arrays (icons, hrefs, screenshots) in components are index-coupled to dictionary arrays — keep orders aligned. |
| `lib/links.ts` | App signup/login URLs — set to `https://app.wazen.fit/signup` + `/login` (D1). Live click-test still on the launch checklist. |
| `lib/screenshots.ts` | Locale-aware screenshot map: every entry is `{ en: {src,width,height}, ar: {…} }` with true per-locale dimensions (EN desktop 2560×1600, AR desktop 3840×2400, mobile 1170×2532 per D9). Captures live in the delivered tree `public/Wazen-Screenshots/{Coach\|Client} Dashboard/{Desktop\|Mobile}/{en\|ar}/<Name>.png` (D13) — all path knowledge stays in this file. |
| `components/ui/Logo.tsx` | The brand mark in nav + footer — the real Wazen mark inlined as `fill="currentColor"` (Pass 2.1, D23). Its color rides the `--color-logo-mark` token in `globals.css` (teal light / sage dark); tones: `ink`, `white` (dark-in-both-themes surfaces), `onPhoto` (transparent nav). Master SVG set in `public/brand/`. |
| `lib/brand.ts` | Mark path data + fixed logo colorways for token-less Satori contexts only (`app/icon.tsx`, `app/apple-icon.tsx`, `lib/og.tsx`) — the sanctioned exception to no-hex-outside-globals. |
| `lib/og.tsx` | Shared OG/social card builder consumed by the EN and AR `opengraph-image.tsx` routes. |
| `lib/video.ts` | Product-tour video single source of truth (D20): per-locale src map, poster, `CHAPTERS` start timestamps (index-coupled to `t.video.chapters`), duration. The real video lands by editing only this file. |
| `lib/flags.ts` | `SHOW_VIDEO_SECTION` — hiding the placeholder video section at merge time is this one line. |

**i18n / RTL:** two route groups — `app/(en)/` serves `/` and `app/(ar)/ar/` serves `/ar`; `RootShell` sets `lang`/`dir` server-side and a pre-paint init script prevents RTL/theme flash. Components use logical properties (`ms-`/`me-`/`start-`/`end-`) throughout; the BrowserFrame address bar is deliberately forced LTR. Runtime language toggle re-points `<html>` attributes without navigation.

**Theme:** hand-rolled (NOT next-themes): class-based `html.dark`, persisted in `localStorage` (`wazen-theme`), applied pre-paint by the init script, toggled in `Nav.tsx` via `useSyncExternalStore`. Dark mode overrides only neutral tokens in `globals.css`; brand hues stay.

**Code guardrails** (carried from the original build spec — still binding):
- No hardcoded hex colors — design tokens in `globals.css` only.
- `next/image` for all raster images; explicit dimensions; hero uses `preload`.
- Animate only `transform`/`opacity`; shared motion vocabulary lives in `components/motion.ts` (`fadeUp`, `staggerContainer`, `scaleIn` for photos); page wrapped in `<MotionConfig reducedMotion="user">`.
- One component per section in `components/sections/`; reusable primitives in `components/ui/`.
- White-label per the D34 boundary: **in-app coach branding (logo, name, theme) is real today and may be shown as available; custom URLs, branded emails, and any standalone-branded-app framing are later and must never be implied.** Composites may showcase in-app branding via a fictional demo coach — never captioned as a real customer, never given a name/quote/testimonial. (Supersedes the pre-D34 "white-label is 'coming soon' — never shown as available" rule.)

**Deployment:** Vercel. Branch pushes get preview deployments; `main` is production **wazen.fit**. **Phase 2 shipped July 2026 via an owner-approved direct `--no-ff` merge of `feat/phase-2-design` into `main` (merge commit `09dfaee`)** — this superseded the originally planned path through `feat/landing-revamp`; both feature branches are retained for history, neither receives new work. Future work: new feature branch off `main`, merged on owner approval. Never commit to `main` directly except by explicit owner instruction (e.g. docs-only follow-ups).

---

## 3 · Key learnings — do not regress

1. **The i18n remount bug (bit us twice).** Motion elements inside a `whileInView`/`once` parent must **never be keyed by translated strings**. On language switch the key changes → the element remounts → it mounts at the `hidden` variant inside a parent whose reveal already resolved → it stays invisible forever. This one root cause produced both an "unexplained maroon bar" (the Problem section's error-tinted card rendering empty) and invisible WhyWazen pillars. Use stable index/structural keys; in-code comments exist at both sites (`Problem.tsx`, `WhyWazen.tsx`). Full-section remounts (e.g. FAQ's `key={lang}`) are safe — it's child-level remounts that break.
2. **Satori (OG images) scrambles wrapped Arabic.** Word order *within* a line is correct, but line-wrapping reorders words across lines. Arabic copy in `lib/og.tsx` ships as **pre-broken lines** that never soft-wrap. Also: Satori rejects `undefined` style values (omit keys instead) and needs an Arabic font loaded explicitly (Tajawal TTF fetched at build with a soft fallback). *Post-2.1 addendum:* the within-line claim re-verified ✅; separately, Satori measures Arabic advances as if unshaped but draws them shaped, so heavily-joined words carry trailing slack (e.g. ~106px beside التدريبي) — cosmetic, identical across native strings/word-spans/diacritic-stripped tests, not fixable while Tajawal is pinned (see the note in `lib/og.tsx`).
3. **Segment-level OG image files get hashed URLs** (e.g. `/ar/opengraph-image-1kyplq`). Never hardcode OG image paths in metadata — rely on the file convention to inject `og:image`/`twitter:image` (see `app/(ar)/ar/opengraph-image.tsx` + `twitter-image.tsx`).
4. **`globals.css` is edited by full rewrite, not string replacement** — targeted replaces have historically failed on this file. Prefer writing the whole file when touching tokens.
5. **CSS `aspect-ratio` transfers `min-height` into a min-WIDTH.** `min-h-[280px]` + `max-lg:aspect-[3/2]` gave the WhyWazen photo slot a transferred 420px min-width on phones: the mobile layout viewport expanded to 420px, and Chrome's `srcset` selection inflated so **every** image on the page fetched its largest variant (`w=3840`) — which is also why huge AR captures rendered blank in slow conditions. Scope `min-h-*` to breakpoints where no `aspect-ratio` rule applies (fix: `lg:min-h-[280px]`, C.2b).
6. **Full-viewport hero images are excluded from Chrome's LCP candidates** (treated as backgrounds), so the photography hero's LCP element is the **headline text** — which must paint pre-hydration: Framer SSRs `hidden` variants as `opacity: 0` (reveal gated on JS), and content revealed by an opacity 0→1 animation never emits an LCP entry at all. Hence the hero copy uses a **CSS-only entrance** with a translate-only variant for the h1/subheadline (`.hero-enter-move`, Pass 2.1). Measure LCP in **observed devtools-throttling mode** — Lighthouse's default *simulated* mode on localhost chains text-LCP behind the entire font/JS graph and overstates it. Observed 4G, Phase 1 → Pass 2.1: EN 6.6s → 2.1s, AR 5.9s → 2.0s.

---

## 4 · Decisions log & standing constraints

| # | Decision | Rationale |
|---|---|---|
| D1 | App URLs live in `lib/links.ts` (single source of truth). **Set (July 2026): `https://app.wazen.fit/signup` + `/login`** — placeholder era over; remaining launch step is the live click-test | The originally assumed domain didn't resolve, so CTAs shipped against a placeholder until the real subdomain was confirmed. |
| D2 | Binaa Labs on the landing page: **footer copyright + FAQ Q6 only** | Studio credit repeated across the chrome read as "agency site" — the strongest wrong signal the audit found. Legal pages keep Binaa Lab (legal name). |
| D3 | Two new sections: segments band + client-app showcase | Answer "who is this for" and "clients get a real app" visually — Everfit-pattern persona segmentation + phone-screen row. |
| D4 | Messaging = 5th Features tab | Messaging was claimed everywhere and shown nowhere; a tab keeps the section architecture instead of adding a new section. |
| D5 | Data-retention windows are **15/45/90 days** (Starter/Pro/Premium) | The FAQ was right; the old build-spec's 90/180/365 was stale. |
| D6 | Pricing: manual **USD/AED toggle**, no geo-IP; AED defaults on `/ar`, USD on `/en`; AED 180 / 365 / 1,800 / 3,635 | Gulf-first audience expects AED; manual toggle avoids geo-IP complexity and wrong guesses. Fixed launch prices, not live peg conversion. |
| D7 | **No women in any photography — hard rule** | Regional market requirement. Applies to every sourced photo including background subjects; verify visually before use. Client *names* in screenshot demo data are unaffected (data, not photography). |
| D8 | Client app is a **PWA** — no app-store badges; copy says "works on any phone, nothing to download"; JSON-LD `operatingSystem: "Web (PWA)…"` | Native apps come later; the page must not overclaim. The "Installs like an app — no App Store needed" chip is the honest framing. |
| D9 | Phone captures via Chrome DevTools emulation, **390×844 @ DPR 3**, EN + AR | Authentic for a PWA — DevTools capture *is* the real client experience. |
| D10 | AED add-on bundle prices: $12→AED 45, $20→AED 75, $18→AED 65 | Owner-rounded from peg math (44/73/66) to cleaner price points. |
| D11 | Pass B demo data via backend seed script (`SEED_DEMO=true`, wazen-backend); featured client Omar Saleh (عمر صالح); EN/AR datasets via `SEED_LOCALE=ar`, one at a time — captures per locale require re-seeding | Repeatable verified data (compliance 55–100%, 8-msg featured thread, QA scans); AR variant makes AR screenshots fully Arabic. |
| D12 | Wazen app i18n hardening (~175 strings keyed, RTL bidi fixes, AR plural sets, locale formatters, Latin digits) as Pass B prerequisite. Branches: wazen-frontend `feat/i18n-hardening`, wazen-backend `feat/demo-seed-ar`. See wazen-frontend `docs/I18N-HARDENING-CHANGELOG.md` | AR captures would have leaked English UI otherwise. |
| D13 | Screenshot naming: keep the delivered tree `public/Wazen-Screenshots/{Coach\|Client} Dashboard/{Desktop\|Mobile}/{en\|ar}/<Name>.png`; all path knowledge in `lib/screenshots.ts` | Re-captures drop in without rename churn. |
| D14 | Messaging tab uses the real Coach Messages capture (supersedes the D4 skeleton note) | Capture now exists; fake next to real reads wrong. |
| D15 | Photo set v1 (F-2/3/5/6/7/9, Unsplash/Pexels free license) shipped as **TEMPORARY** — flagged for later refinement/own shoot. F-7 chosen over F-10 as the regional signal. F-5's partial second hand (nail polish at frame edge) is a D7 concern **accepted by owner** for v1; the crop trims most of it. F-6 carries a small background poster + laptop sticker — noted for the v2 swap. F-9 pre-darkened/desaturated in the export so the 12% CTA backdrop reads as texture, not subject. | Real photography now beats placeholders for launch; stock is good enough for v1 and gets replaced by Gulf-authentic shots post-launch. |
| D16 | SEO baseline (July 2026): category-led `<title>` + meta description on both locales — EN targets "coaching platform" / "personal trainer software", AR targets منصة تدريب / برنامج للمدربين; `sizes` added to the two unsized dashboard images (hero, Features sync panel); sitemap `lastModified` bumped. og/twitter share titles keep the promise-led wording. Owner declined the Terms-page "software studio"→"product company" edit — legal wording stays as-is. | Audit found metadata/hreflang/sitemap/robots/JSON-LD/headings/alts already complete and Lighthouse SEO already 100, but no category keyword appeared anywhere Google weighs (only in og/twitter/keywords tags), and the unsized images served a single `w=3840` srcset candidate to phones (~89 KB where ~23 KB suffices — LCP). Verified post-change: build clean, Lighthouse SEO 100/100 EN + AR. |
| D17 | Phase 2 hero = **Option A**: full photography-led hero with a live 3-persona slider; the Phase 1 dashboard+phone composite relocates to a new **product-proof section** ("Your whole practice, one screen") below the video section | Owner reviewed both mockup renders and picked A — strongest 3-second category signal, cleanest layout, best LCP; the composite lands one scroll later at full width, keeping the Phase 1 investment. |
| D18 | Hero H1 serif accent: **Fraunces italic** (EN, loaded via next/font on the EN route only, off the preload critical path) in pale sage; **AR = Tajawal 800** in the same sage — no italic and no Fraunces on Arabic, ever. The conditional keys off the **live language state**, not the route (runtime toggle re-points `<html>` without navigation) | The editorial serif is the hero's signature (replaces Phase 1's wavy underline). Arabic has no italic tradition — weight/color contrast in the native face instead of a faux-slant. `/ar` toggled to EN degrades to Georgia italic until navigation (accepted). |
| D19 | Primary CTA on the dark hero = **sage pill + darkest-teal text** (`bg-secondary text-primary-darker`, token-driven Button `variant="sage"`); secondary is a glass ghost; light-section CTA styling unchanged | Deep-teal-on-photo lacks contrast; the sage pill is the brand accent at full strength and passed the owner's mockup review. |
| D20 | Video section ships **visible in placeholder mode** behind a one-line flag (`lib/flags.ts` → `SHOW_VIDEO_SECTION`), positioned directly after the hero; wired against a 2:04 silent dark-teal dummy MP4 so the timeupdate→chapter-rail sync is real; poster = F-9 under the section scrim; real video lands as a file swap + 4 timestamps in `lib/video.ts` | The real walkthrough is in production with an editor. Building the full mechanic now makes launch an asset swap, and a visible placeholder lets the owner judge the section in context (chapter timestamps 0:00/0:34/1:12/1:41 match the approved mock). |
| D21 | Features tab **auto-advance approved** (progress rail doubles as the timer; stops on first interaction) — implementation deferred to Pass 2.2 | Belongs with the pills→progress-underline tab restyle, which is 2.2 scope. |
| D22 | Photo strategy v2: **AI-generate the three hero personas**, stock for no-people slots, per PHASE2-PHOTO-SOURCING.md; **F-7 re-crop stands in for all three hero slides until Pass 2.3**; C-1 commissioned, with F-9-at-12% as the live CTA fallback | D7 (no women anywhere in frame, including backgrounds) is nearly impossible to guarantee in stock gym photography; generation controls subject, background emptiness, and headline negative space. |
| D23 | Logo set in `public/brand/` from the stripped-background master (tight `viewBox`, single path — **never reintroduce padded/background versions**); currentColor mark inlined in `components/ui/Logo.tsx`; **dark-mode mark color = one token (`--color-logo-mark`) defaulting to sage `#7BA898`**; light mode teal `#3D8C84`; on-photo nav uses light `#F0F0ED`. **Sub-note (owner verdict, 2.1 review): the 16px favicon uses a simplified W-stroke — the dumbbell detail is illegible at 16px; the full mark is used everywhere ≥32px** (favicon only — the mark itself is untouched). | Owner hard requirement: the mark stays in the brand green family in dark mode. The single token makes the sage↔teal call a one-line edit for in-browser judgment during 2.1 review; owner confirmed the default as built. |
| D24 | Nav = full-width bar, **transparent only over the hero photo** (on-photo logo colorway, glass CTA pill), solid `bg` + `shadow-sm` after ~80px of scroll and on every non-hero surface (`variant` prop, defaults to solid) | A floating pill can't sit on photography; the full-width two-state bar is what the approved Option A mockup shows. Terms/Privacy render no Nav at all, and the default-solid prop encodes the rule for any future mount. |
| D25 | **"Log in" nav link added** → `app.wazen.fit/login` (`lib/links.ts`), quiet text treatment in both nav states + the mobile menu | Introduced by the approved mockup and accepted by the owner — returning coaches need a path that isn't the signup CTA. |
| D26 | **Problem redesign = chaos→calm split** (Stage-2 mock approved): oldWay copy as scattered rotated notes over F-2 duotone converging via drawLine into one real attention-queue fragment + wazenWay rows; sage glow + ghost wordmark stay | Strongest use of the section's existing copy — the argument becomes spatial, the photo gets a job, and the twin-card skeleton dies where it's most visible. Answers Stage-2 checklist #1. |
| D27 | **Problem mobile = chaos-above/calm-below** with a downward converge connector; F-2 appears on mobile for the first time | Mobile finally gets people in this section, and converge-downward matches scroll direction. Checklist #2. |
| D28 | **Features panel = composed scene** (wash + ghost chapter number + 2 real-capture fragment chips + phone breaking the wash edge) on top of the settled D21 rail tabs; **pill icons die with the pills** (text-only labels). Features-mobile had no mock — the 390px pre-commit screenshots are its approval render | The ×5 flat screenshot surface was the page's biggest craft gap; fragments are real-capture crops so nothing is invented. Checklist #3. |
| D29 | **Features tab dwell = 6s**, matching the hero persona rail — one rhythm page-wide. Named constant in `Features.tsx` (`DWELL_MS`); **owner flagged for possible revisit at review** — the change is a one-line edit | Hero-rail precedent beats Stage-1's sketched ~8s. Checklist #4. |
| D30 | **Photo divider ① approved** between Features and HowItWorks; **D-1 brief activated** (already specced in PHASE2-PHOTO-SOURCING.md); F-4-era stand-in acceptable until sourced. Ships in Pass 2.2b | Cheapest fix for the page's longest people-free run. Checklist #5. |
| D31 | **HowItWorks redesign = zigzag timeline** along a drawLine spine, three distinct visual objects (floating invite modal fragment · tilted plan phone · frameless chart strip), desktop + mobile collapse as rendered. Ships in 2.2b | A sequence shaped like a sequence; the section stops competing with Segments' grid. Checklist #6. |
| D32 | **Segments redesign = persona mosaic** (copy ON duotone photos, asymmetric large+stacked split, mobile stacking as rendered) **subject to refinement R1: per-panel bottom scrim must reach AA over the LIGHTEST region of each photo, verified per photo** ("Nutrition coaches" over F-5 borders failure in the mock). Ships in 2.2b | Makes the hero↔segments persona rhyme visible instead of coincidental; same contrast-requirement class as the hero persona labels. Checklist #7. |
| D33 | **ClientApp redesign = cinematic dark band** (`primary-dark→darker`), **D-2 brief activated** as the low-opacity duotone backdrop, outer phones angled ±4° with phoneFloat idle; owner note R3: vet D-2 candidates at backdrop opacity under the scrim before committing, and reassess the two-white-phones-read-clinical concern only after the real D-2 lands. Ships in 2.2c | The page's second half needs its dark beat; phones read dramatically better on dark. Checklist #8. |
| D34 | **White-label truth boundary + claim wording (Option A).** In-app coach branding (logo, name, theme) is REAL today and may be shown as available; custom URLs, branded emails, and standalone-branded-app framing are LATER and must never be implied. Claim copy: EN "Your brand, front and center — clients see your logo, name, and colors in their app." · AR "علامتك في الواجهة — يرى عملاؤك شعارك واسمك وألوانك داخل تطبيقهم." Showcased via a fictional demo coach on the ClientApp third phone (new capture brief) — never captioned as a real customer, no name/quote. **Supersedes the §2 guardrail "white-label is 'coming soon' — never shown as available" (guardrail rewritten).** Pricing untouched: Premium's "Basic branding customization" + "Full white-label — coming soon" note remain accurate (D6). Code ships in 2.2c | Benefit-first wording whose scope ("in their app") self-limits the claim to the truth boundary. Checklist #9. |
| D35 | **WhyWazen redesign = editorial collage**: numbered pillars with the Fraunces-italic accent (AR = Tajawal 800 sage, D18), layered F-7 + coach-profile frame + founder pull-quote overlapping the frame's stat row (deliberate — full capture stays one lightbox-click away). Ships in 2.2c | Highest-craft treatment for the page's conviction moment; the serif accent gets its one echo. Checklist #10. |
| D36 | **CTA recomposition approved as mocked** (owner-pre-seeded OG-card composition over the current F-9 + wordmark backdrop; composite bleeds off the end edge; MiniDashboard skeleton dies — D14 class closed); **`cardTitle`/`cardSub` dictionary keys retire in the CTA pass (2.2c)** | Composition already proven by the OG card; trust rows cover the retired card copy. Checklist #11. |
| D37 | **HowItWorks plan-phone keeps the 9/19 frame crop.** *Premise correction (2.2a verification):* the footer is NOT hidden by the 9/19 window — `clientMobilePlans` has short page content, so "Powered by Binaa Labs" sits at ~61% of the capture's height and no phone-shaped crop excludes it (it has been faintly visible in the shipped Features PiP since C.2a). The 9/19 standard stays (it's the correct phone geometry); the footer itself is a **capture defect** — queued in the re-capture ledger, resolution per the owner's item-4-class decision (interim raster edit or wait for the 2.3 re-capture) | Zero work and D2-clean as a crop rule; the real fix moves to the capture layer. Checklist #12. |
| D38 | **Re-capture ledger confirmed** (PHASE2-STAGE2-DESIGN.md §5): every slot tagged keeps-current / keeps-current-fragment (crops noted) / new-capture; the only NEW capture is the white-label demo-coach client Home. The ledger is the fixed shopping list for the final re-capture pass (2.3) | Freezes scope for the one remaining capture session. Checklist #13. |
| D39 | **Keep tier confirmed: Pricing, FAQ, Footer untouched** through Phase 2 | Pricing is the calm palate-cleanser between two dark beats (D6); FAQ's accordion is the right shape; Footer is a quiet close (D2). Checklist #14. |
| D40 | **Features tabs 2/3 panel captures swapped to coach view-client pages** (Pass 2.2a.1): tab 2 `coachDashboard`→`coachViewCheckin` (`View_Client_Check_in`), tab 3 `clientProgress`→`coachViewProgress` (**addendum, Pass 2.2a.2:** final state is `View_Client_Progress_2` presented BOTTOM-anchored — the visible panel region is the "Adherence & Wellness Averages" card per the owner's reference crop, via a `cropBottom` flag on the tab meta; the Lightbox opens the full capture. The interim `_1` state was a miscommunication resolved the wrong way); new `lib/screenshots.ts` entries with per-file-verified dimensions (EN 2560×1600 / AR 3840×2400, sharp metadata). Chip re-check: no duplication on either tab — pairings unchanged. §5 ledger inherits both slots (interim; proper coach-side Check-ins/Progress captures at 2.3) **with the note: BrowserFrame URL mismatch (`/dashboard`, `/progress` vs view-client pages) resolves at re-capture** | Owner review of the 2.2a preview: the tab copy tells coach stories ("you review everything from one organized queue") while the shipped captures showed client views. |
| D41 | **Default pricing currency = USD on BOTH locales** (July 2026). **Partial supersession of D6:** the AED-defaults-on-`/ar` clause is superseded by owner decision; D6's manual-toggle-only, no-geo-IP, and fixed-AED-launch-price (with D10 add-ons) clauses remain in force. Implementation: constant `"USD"` initial state; the re-default-on-language-switch effect and its touched-ref removed (an explicit choice now survives language switches by construction) | Owner decision; toggle and all price values untouched, zero i18n changes. |
| D42 | **Segments mosaic photos are decorative (`alt=""`)** — in the D32 copy-on-photo composition the persona semantics live in the on-photo title/body, so the F-3/F-5/F-6 images carry empty alt (no aria-hidden needed). Accepted by owner at 2.2b plan approval; this is what makes the `photoAlt` dictionary keys unused, enabling their queued retirement in the 2.2c i18n pass (`lib/i18n.ts` untouched in 2.2b per scope) | WHATWG guidance: an image whose meaning is fully conveyed by adjacent text is decorative; duplicate alt would be noise for screen readers. |
| D43 | **Dictionary orphans deferred (2.2c):** `clientApp.phones[2]` ("Progress" — its phone slot became the white-label story) and `problem.photoAlt` (orphaned in 2.2a when F-2 went decorative; found at 2.2c audit) remain in the dictionary — the pass's i18n change list was exhaustive by owner instruction (exactly four changes). Queued for retirement in the next i18n-touching pass alongside any 2.3 string work | Scope discipline beats opportunistic cleanup: unenumerated dictionary edits are how shape drift and missed-consumer regressions happen. |
| D44 | **CTA mobile bleed removed (2.2c.1). Partial supersession of D36:** below `lg` the composite renders fully inside the content width (`ms-3.5` accommodates the phone's −14px start overhang; no negative end margin) — the owner's device review read the mobile bleed as an accidental crop, and the diagnosis showed the real culprits (composite scale + the clip edge bisecting content) can't be fixed at phone scale without fighting the format. **Desktop keeps the D36 bleed exactly as shipped** (`lg:-me-45`, section overflow-hidden stays); §6's off-edge-bleed-is-CTA-exclusive rule now applies to desktop only | The bleed vocabulary needs enough width to read as deliberate; at 390px it never will. Owner pick over the proposal round's in-frame-zoom option. |
| D45 | **WhyWazen mobile = Option A "pulled-up overlap stack" (2.2c.1)** — closes the 2.2c mobile-mock gap (the Stage-2 whywazen mock was desktop-only): photo full-width 3:2 → frame 92% start-aligned pulled −64px over it (z-1) → quote 88% end-aligned pulled −48px over the frame's bottom (z-2, the stat-row overlap survives the collapse) → annotation in flow. Pure flow layout (no absolute positioning), `max-lg`-scoped classes, desktop pixel-identical | Owner pick from three rendered options (A/B/C, `_preview/pass2.2c1/`); flow layout means AR text growth can never overflow a fixed container. |
| D46 | **Hero photos = no-people environment scenes (2.3a). Partial supersession of D22:** the persona-generation clause (AI-generate three hero personas WITH subjects) is superseded — the shipped H-series are environment scenes (H-2 meal-prep counter · H-3 practitioner desk, both 1856×2304 AI masters; **H-1 = 1920×1280 stock gym interior, swapped at the render gate** after the AI master's featureless right wall read as a crop defect at 16:9 — the owner's replacement, same filename). D22's stock-for-no-people-slots and F-9-fallback clauses stand; people remain on the page via D-1, F-3/5/6, F-7. **Gate verdict record:** slides 2–3 approved as wired; H-1 v2 approved via the delegated checks (composition full-frame both locales, no scrim strengthening needed on any hero slide); plate weight markings accepted as generic. **Photo note:** the retired H-1 AI master carries a faint manufacturer mark on the rack base (illegible at rendered scale + duotone — any future re-crop must re-verify); the H-1 v2 master carries a heavily occluded dark-on-dark wall sign + tiny shelf stickers, all illegible at native resolution (verified in the 2.3a lettering zooms); it was intended to be retired to `_photo-originals/` as a spare but was overwritten in place before this pass ran — not recoverable from the repo (owner holds any surviving copy). | The 3-second category signal survives without faces; environment scenes dodge the D7 stock-photography risk entirely and the AR-side composition (copy over each scene's empty side) turned out stronger than the person-in-frame sims. |
| D47 | **D-1 + D-2 narrative mismatches accepted as v1 (2.3a, D15-swappable class):** D-1 is battle-ropes mid-workout action (brief: resting between sets, phone in hand); D-2 is a gym deadlift close-up, no face/phone (brief: man at home post-workout, morning light). Both male-only incl. backgrounds (D7 verified: the D-1 background is empty; the D-2 frame is legs+bar; the H-1 v2 mirror reflections are equipment-only — pixel-checked). The v2 photo refinement replaces them against the original briefs if desired | The delivered set ships the people-interleave and the cinematic beat NOW; the briefs stay on file for the post-launch photo pass. |
| D48 | **R3 resolved (2.3a): ClientApp backdrop opacity stays `opacity-55`** — judged against the real D-2 via the rendered triptych (45/55/65, `_preview/pass2.3a/r3-*`; owner confirmation at the 2.3a shot review). The deferred two-white-phones-read-clinical concern is **closed**: against the real deadlift texture the white UIs read as product shots over believable depth, not clinical panels. Divider scrim addendum: D-1's brightness washed out the band as shipped in 2.2b tuning, so the divider gained a uniform `bg-primary-dark/35` veil + strengthened bottom stop (symmetric, RTL-safe) — measured caption contrast after: EN 6.23:1 · AR 7.69:1 | 55 keeps the photo present without competing with the phones; the strengthened divider scrim is the render-gate's sanctioned fix applied where the real photo actually needed it. |
| D49 | **White-label capture shipped (2.3b) — D34 closed.** The ClientApp third phone consumes the real demo capture (`clientMobileWhiteLabel` → `Client Dashboard/Mobile/{en\|ar}/Whitelabel_Home_Tab.png`, 1170×2520 — 20px shorter than the D9 standard, explicit dims in `lib/screenshots.ts`): client Home carrying the FICTIONAL **"Apex Coaching"** brand (own logo, name, navy theme — visibly not Wazen's). Truth boundary held: in-app branding only, no custom domain/email/store framing anywhere in frame, coach never named as real or quoted. Lightbox per the phones-1/2 pattern; alt = the D34 caption (each phone's alt is its caption). **Capture defect note:** both files carry a faint edge scrollbar (~7px, physical right EN / left AR) — invisible at the in-place phone scale (sub-pixel against the frame border), measurably faint in the Lightbox (L≈224 vs 251 band); owner-accepted for launch in the same class as the `clientMobilePlans` footer (ledger-queued for any future re-capture). PhotoPlaceholder lost its final consumer — component kept for future placeholder needs | The last "NEW CAPTURE" surface on the page closes; the fictional brand keeps the no-fabricated-proof rule intact. |
| D50 | **Re-capture ledger deferred post-launch (2.3b).** Every §6 known-capture-defect entry (Templates_Tab DOCTORED interim + AR footer · `clientMobilePlans` mid-screen footer · D40 view-client interim captures + BrowserFrame URL mismatch · per-locale seed stat drift · white-label edge scrollbar) is **owner-accepted for launch** — entries and history notes KEPT as the post-launch re-capture session's shopping list. F-11 founder portrait also deferred: the "NS" initials avatar ships | None of the defects is visible above Lightbox-zoom level at page scale; launch is not blocked on a re-capture session that wants a purpose-built seeded environment anyway. |
| D51 | **Hero slide-1 mobile crop = `object-[75%_50%]` below `md` (2.3b)** — owner pick from the three rendered candidates (55/65/75, `_preview/pass2.3b/hero-mobile-x*`): the 75% slice leans fully into the dumbbell-rack side per the H-1 v2 composition intent; desktop keeps the gate-approved 65% (X is near-inert at 16:9) | The owner-flagged "slightly off" mobile crop centered on the emptier bench zone; 75% puts the racks (the persona signal) behind the copy. |
| D52 | **Pricing truth alignment + tab metadata (2.4; source: Billing_Final.md v3, owner-supplied content; final amendment set + owner rulings).** Correction groups, EN+AR: (a) Premium add-on bundle → "+5 clients from $9/mo · bundles up to +50" with **AED 33** (owner-set, D10 rounding class — supersedes the $18/AED 65 pair); (b) every "scale to 100+"/"100+" → "up to 100" (Premium highlight + feature row + FAQ Q8); (c) "Basic branding customization" moved Premium → Professional (Premium inherits via "Everything in Professional"; the "Full white-label — coming soon" note stays verbatim); (d) **Phase-7 verdict amended: two of five implemented** (report export levels, basic branding) — "Scheduled messages" (Professional), "Advanced automation" + "Suggested follow-up list" (Premium) REMOVED pending implementation; the §6 Phase-7 reminder re-adds all three when gating ships; (e) Premium rebalanced to 6 rows with two doc-sourced rows: "Larger client bundles (+25 and +50)" (Premium-exclusive bundle sizes) + the export-window row shipped as **"Longest data export window"** (no day figure) after the retention truth gate fired — owner ruling: **in-app retention = 15/45/90, verified — D5 STANDS**; Billing_Final.md §3.2's conflicting retention numbers are a doc error the owner corrects separately; the retention FAQ is untouched. FAQ Q8 rewritten (both locales) to match the final 6-row lists. Yearly values verified against the doc: USD $490/$990 ✓ (Pricing.tsx constants); AED yearly 1,800/3,635 unchanged (existing owner-set pair). **Scope extension (owner-ruled): tab metadata** — `<title>` → bare brand "Wazen" (EN, `app/(en)/layout.tsx`) / "وازن" (AR, `app/(ar)/ar/page.tsx`); meta descriptions + og/twitter share titles unchanged (D16 wording kept); icon tile regenerated light-mode (light surface + teal mark via the `lib/brand.ts` constants, `app/icon.tsx` + `app/apple-icon.tsx`) and **`app/favicon.ico` DELETED — the generated light icon is the single source, superseding D23's hybrid-favicon sub-note** (the simplified 16px W-stroke retired with the .ico); viewport theme-color verified as a media-gated light/dark pair (forces nothing) and kept as-is; `app/manifest.ts` untouched. **Amendment (July 2026, owner ruling): icon colorway reverted to the original teal tile (teal surface + light mark) — the light-surface variant is superseded; `app/favicon.ico` remains deleted, the generated icons stay the single source.** **D43 closed:** the two deferred orphans (`clientApp.phones[2]`, `problem.photoAlt`) retired EN+AR with zero-consumer greps | Billing_Final.md is the billing single source of truth for plan structure — but the landing page must never claim features the app hasn't shipped, and where doc and live app conflict (retention), the live app outranks the doc. |
| D53 | **Section labels (eyebrows) removed page-wide (post-launch maintenance).** The small uppercase label above each section H2 is removed from all nine live sections (ProductProof, Problem, Features, HowItWorks, Segments, ClientApp, Pricing, WhyWazen, FAQ) and the flag-off VideoTour; the H2 — or, for ProductProof, its sub — carries each section's identity. `components/ui/SectionHeader.tsx` loses its `eyebrow`/`eyebrowTone` props + the Badge import (7 consumers updated); ProductProof/VideoTour lose their inline bare-text eyebrow; WhyWazen loses its inline Badge eyebrow **and** the orphaned `h2 mt-4` (the other nine headers self-close via their `gap-4` flex container — verified). 10 dictionary keys retired EN+AR — `{productProof,problem,features,how,segments,clientApp,pricing,why,faq,video}.eyebrow` — on zero-consumer greps (D42/D43/D52 precedent). **KEEPS:** `why.founderEyebrow` ("Why we built this") and the two Problem column labels (`oldWayHeading`/`wazenWayHeading`) — the eyebrow treatment survives only where it labels an element nothing else labels. **R6 (RTL fix):** `--text-eyebrow--letter-spacing: 0.08em` is a Latin small-caps device that broke Arabic cursive joining on the survivors; an unlayered `[dir=rtl] .text-eyebrow { letter-spacing: normal }` resets it (uppercase left as-is — a no-op on Arabic); `text-eyebrow` confirmed eyebrow-only before applying. New sections get no label unless the H2 genuinely cannot carry it. Build + typecheck clean; 8 full-page (EN/AR × light/dark × desktop/mobile) + 4 AR survivor crops captured for the monotony gate. | The page opened ten sections with near-duplicate labels restating their H2s in smaller type; subtracting them lets each H2 own its section and removes the RTL tracking defect on the survivors. Supersedes nothing — no prior D-row governed the eyebrow system. Owner-ruled subtractive change; the only open question (monotony of ten bold H2s on flat backgrounds) is a full-page owner judgment, not visible in section crops. |
| D54 | **Hero decluttered for CTA focus.** Badge removed (`hero.badge` retired EN+AR) — its "full platform, no limits" claim also dies as an overclaim: the free tier IS a client-count limit, so the phrase was retired rather than migrated to the trust row (owner ruling; the trust row already carries "Free forever for 5 clients"). Secondary CTA demoted from a glass pill to a **one-off plain text-link anchor in Hero** (owner ruling — no new Button variant; underline, arrow kept) and re-anchored `#how-it-works` (was `#features`; the ruling's literal `#how` has no matching id in the DOM). **Animated 6s progress rail removed — per-breakpoint split after the shot review (R11/R12):** DESKTOP (≥sm) keeps labeled tabs with a **static sage active indicator only** (the fill ran every frame the hero was on screen and competed with the CTA) and no inactive tracks (the full-width rules read as navigation). MOBILE (<sm) is **bars-only**: the active-label line is removed and no text renders at all (persona names stay sr-only + aria-current); all three bars keep a visible base track — they ARE the switcher — and the active bar **restores the scaleX 0→1 dwell fill** (key={cycle}, the original mechanism verbatim): with no mobile text, a static bar communicates nothing. Reduced motion: static full bar (MotionConfig strips the tween), consistent with the existing branch. The label line's orphaned `mt-2` on the tab row went with it. `railFill` stays retired from `components/motion.ts` (zero consumers — Hero and Features both inline their fills; note the correct path, not lib/motion.ts). Primary CTA shortened "Start free — up to 5 clients" → **"Start free"** EN+AR (the qualification lives in the trust row) with **padding B** (`px-9 py-4` override) per the render gate. **Mobile trust row trimmed to its first two items** (R13: `max-sm:hidden` on "Arabic & English"; the key stays — desktop renders all three; the two survivors wrap to two lines below ~390px — measured 340px of content at 360px vs ~312px available — **reviewed and accepted at fringe viewports**, one clean line at 390px+). **CTA→trust gap tightened one token step** (R14): the copy block's shared `gap-5` (20px) can't step per-pair, so the trust row carries `-mt-1` → 16px effective (gap-4 equivalent), all breakpoints, both locales. **AR tab-label tracking reset** via per-element `rtl:tracking-normal` — same defect class as D53's R6, but `tracking-[0.16em]` is an arbitrary value outside the `text-eyebrow` token, hence per-element utilities, not the CSS rule; R11's removal of the mobile label line took one of the two reset sites with it, leaving the tab buttons (the only remaining AR tab text, ≥sm); the AR H1's negative tracking is untouched (reported, not ruled). **First-load Ken-Burns defect fixed (R15):** slide 1 mounted with `initial={false}`, which resolves the kenBurns keyframes to their final value — the first photo sat fully static for the entire first dwell and only drifted from rotation 2 onward (verified by runtime transform sampling: `transform:none` across t=0.8–5.5s, slide 2 mid-drift at t=7.5s). Hero-wiring fix only, `motion.ts` untouched: slide 1 gets an explicit `initial={{ scale: 1.06 }}` (SSR pre-paints the drift's start; hydration plays the same 1.06→1 drift every slide gets, no pop) guarded by `motion-reduce:transform-none!` so reduced-motion users keep a fully static slide 1 from first paint (the !important beats the SSR inline transform). **Carousel rotation, DWELL_MS (6s), crossfade, the kenBurns vocabulary, and click-rebase are UNCHANGED and ruled correct:** decorative rotation re-bases on interaction, content rotation stops (cf. D21) — this asymmetry is intentional, do not "fix" it. Reduced motion verified coherent post-change (static slide 1, static indicator, manual tabs). Amends the D17/D29 composition; does not supersede them. | Nothing read as primary next to a twin pill, and the rail was the composition's dominant motion; subtracting both drives the eye to the single sage CTA. The badge line doubled the trust row and overclaimed. |
| D55 | **Em dashes removed from user-visible copy, per the diagnostic pass's per-row inventory** (EN + AR 1:1: Arabic comma "،" where EN got ",", Arabic semicolon "؛" for ";", colon/period/parenthetical rewrites mirrored; Latin digits untouched). Reaches the dictionary, page metadata (og/twitter titles → ":"), `app/manifest.ts`, `lib/seo.ts` (meta descriptions + JSON-LD `operatingSystem`), the OG-card sub lines (`lib/og.tsx`, both locales — pre-broken AR lines only got shorter, no wrap risk), and the legal pages (titles → " \| "). `cta.ctaLabel` shortened to "Start free"/"ابدأ مجانًا" — identical to the D54 hero primary (owner ruling; the CTA body + trust row carry the 5-clients qualification). **Pricing note (D52 amendment, owner-gated):** the directed replacement listing "exercise library & AI assistant" as coming for Premium **failed the no-unimplemented-features gate** — zero evidence in the app repos, their billing docs, the phases roadmap, PROJECT.md, or the design docs — so the note was repunctuated only: "Full white-label branding — coming soon" → **"Full white-label branding, coming soon"** (AR "علامة بيضاء كاملة، قريبًا"); the feature-list wording waits for real evidence. **pwaChip (D8 amendment):** the quoted claim's punctuation changed ("Installs like an app, no App Store needed"), wording intact; same for D8's `operatingSystem` string. **Surviving copy dashes, exhaustive:** `clientApp.whiteLabelCaption` EN+AR — the " — " is load-bearing (`ClientApp.tsx` splits on it for the bold lead) and D34-pinned verbatim (the sole em-dash exception); the two held headings `problem.h2` + `clientApp.h2` EN+AR (owner ruling — heading rewrites deferred); terms' "days 4–10" en dash (legitimate range). **New rule: no em dashes in UI copy; en dash for numeric ranges only.** | The dash had become the page's default connective — commas, colons, and sentence breaks read calmer and translate cleanly; Arabic never idiomatically used em dashes (they were translation artifacts, mirrored out). The coming-soon gate held: a "coming soon" claim is still a feature claim. |

**Standing constraints:**
- **No fabricated testimonials or social proof.** Pre-launch startup: the proof band uses honest claims ("Built with founding coaches…"), the coach-profile screenshot is the product-proof visual, and testimonial slots stay empty until real pilot coaches exist. Review-platform badges are a post-launch item.
- **No women in photography** (D7) — repeated because it governs all future sourcing.
- Pricing is USD/AED via the manual toggle only (D6), **defaulting to USD on both locales (D41)**; FAQ prose quotes USD.
- Binaa Labs naming rules per D2; "software studio" phrasing is banned on the landing page (use "product company"). Exception: the Terms page keeps its existing "software studio" wording by owner decision (D16) — don't "fix" it.

---

## 5 · The revamp — why and what

**The audit's finding (July 2026):** the page failed the 3-second test — it read as a custom-software agency site, not a coaching SaaS. Not because visuals were missing (12 real screenshots already rendered) or the copy was service-y (it was already product-SaaS), but because of five gaps:

1. **Zero human beings** — no photography, no faces, no fitness context anywhere.
2. **Zero social proof** — and the trust bar spent the proof slot on self-claims including "Powered by Binaa Labs."
3. **Invisible category signal** — desktop admin screenshots shrunk to illegibility; the client app never shown as a phone.
4. **QA test data in shipped screenshots** — `terminated.client@example.com`, 0% compliance, "USED BY 0", a `localhost:5173` badge — all zoomable via lightbox.
5. **Dead conversion URLs** — every CTA pointed at a domain that doesn't resolve (→ D1; resolved July 2026: `app.wazen.fit`).

Benchmarks: Everfit (hero product UI + proof band + persona segmentation + phone rows), Trainerize (CTA friction microcopy), PTDistinction (desktop+phone hero pairing).

### Page structure (target state)

Type key: **[a]** product visual (device frame) · **[b]** photography · **[c]** graphic/skeleton.

| # | Section | Visuals | Status |
|---|---|---|---|
| 1 | Nav | real logo mark [c]; transparent over hero → solid on scroll (D24) | ✅ restyled 2.1 |
| 2 | Hero | full-bleed photography + persona slider [b] + serif accent (Option A, D17/D18) | ✅ rebuilt 2.1 — F-7 stand-in ×3 until 2.3 |
| 2b | Video tour | 16:9 stage + 4 playback-synced chapter cards [a+c] (D20) | ✅ placeholder mode, behind flag |
| 2c | Product proof | browser frame: coach dashboard [a] + overlapping phone frame [a] + annotation chips [c] (relocated Phase 1 hero composite) | ✅ new 2.1 — real captures |
| 3 | Proof band | honest text proof, compressed to a quiet strip attached to product-proof; pilot coach headshots later [b] | ✅ restyled 2.1 |
| 4 | Problem | chaos→calm split: scattered notes over F-2 duotone → drawLine converge → queue fragment [b+a+c] (D26/D27) | ✅ redesigned 2.2a |
| 5 | Features — 5 tabs | progress-underline rail tabs + composed scene: wash, ghost number, browser capture, breakout phone, fragment chips [a+c] (D21/D28) | ✅ redesigned 2.2a |
| 5b | Photo divider ① | ~33vh full-bleed D-1 band between Features and HowItWorks, one overlay line reusing tabs[1].caption [b] (D30) | ✅ new 2.2b — PhotoPlaceholder until D-1 sourced (2.3) |
| 6 | Coach ↔ Client sync | desktop frame + phone frame + animated flowing-dots connector [a+c] | ✅ real captures |
| 7 | How it works | zigzag timeline on a drawLine sage spine; three distinct objects: invite-modal fragment · tilted plan phone (9/19, D37) · frameless chart strip [a] (D31) | ✅ redesigned 2.2b |
| 8 | Segments band | asymmetric persona mosaic, copy ON duotone photos over per-panel AA scrims [b: F-3/F-5/F-6] (D32 + R1) | ✅ redesigned 2.2b |
| 9 | Client-app showcase | cinematic dark band: D-2 duotone backdrop [b] + 3 phones ±4° on phoneFloat — check-in · home (raised) · white-label NEW-CAPTURE slot with the D34 claim caption [a+c] (D33/D34) | ✅ redesigned 2.2c — D-2 + white-label capture at 2.3 |
| 10 | Pricing | USD/AED + monthly/yearly toggles, tool icon chips [c] | ✅ |
| 11 | Why Wazen | editorial spread: numbered serif-accent pillars (D18) + layered collage — F-7 duotone panel, coach-profile frame, founder pull-quote overlapping [b+a+c] (D35) | ✅ redesigned 2.2c — F-11 portrait pending |
| 12 | FAQ | text accordion | ✅ |
| 13 | CTA | OG-card composition: copy + 2×2 trust rows + sage pill start-side; real dashboard-in-browser + check-in phone bleeding off the end edge [a+b] (D36; F-9 backdrop @12% unchanged) | ✅ redesigned 2.2c — MiniDashboard skeleton retired |
| 14 | Footer | logo mark, single Binaa Labs line, support@wazen.fit | ✅ |

### What shipped

- **Pass A — foundation (code-only):** Binaa Labs consolidation; hero composite (tightened rhythm, PhoneFrame + branded skeleton screens, translatable annotation chips); proof band; 5th Messaging tab; USD/AED pricing toggle with per-locale defaults and per-currency savings; standardized CTA microcopy; PWA copy corrections EN+AR + JSON-LD; WhyWazen counters removed (they mimicked scale metrics); Logo component; dark-mode screenshot softening; AR SEO fixes; `links.ts` launch TODO.
- **Pass A.1 — defect fixes:** the i18n remount root-cause fix (learning #1); content-anchored hero chips (physical coords while captures are LTR); densified messaging skeleton; flowing-dots sync connector with reduced-motion fallback; plan-like "today" skeleton; value-strip icon chips; AED add-on prices (D10).
- **Pass C.1 — structure with placeholders:** `lib/screenshots.ts` locale map wired through every consumer; segments band built; `PhotoPlaceholder` component (branded, F-labeled, true final aspect) placed in all photo slots; coach-profile proof point in WhyWazen; OG composite cards EN+AR (learnings #2/#3).
- **Pass C.2a — real capture swap:** all product screenshots replaced with the new Wazen capture set (curated demo data, new in-app logo, native AR captures — no more EN-on-/ar). 14 map entries (9 desktop + 5 client-mobile) with true per-locale dimensions; every `PhoneSkeleton` replaced by real PWA captures via the new `PhoneShot` component (hero, features PiPs ×5, sync panel, showcase ×3, CTA composite); messaging desktop tab got its real capture (D14); invite-modal brightness hack removed (new capture has a bright backdrop); all 20 old QA-data PNGs pruned from `public/screenshots/`; OG cards auto-picked-up the new dashboard captures. P0-3 (QA data) and P1-3 (EN screenshots on /ar) are closed. Demo data came from the D11 seed script after the D12 i18n-hardening prerequisite.
- **SEO baseline pass (July 2026, D16):** full audit of metadata, hreflang, robots/sitemap, JSON-LD, headings, alts, and CWV risks against the rendered production HTML — nearly everything was already in place (single h1, valid heading ladder, zero missing alts, complete hreflang both directions, JSON-LD @graph with Organization/WebSite/SoftwareApplication/FAQPage, hero preload, Lighthouse SEO 100 both locales). Changes shipped: owner-approved category-led `<title>` + meta description EN + AR (see D16), `sizes` on the hero and Features-sync dashboard images (phones were fetching the `w=3840` variant), sitemap `lastModified` bump. Known-benign: the `metadataBase` build warning comes from the auto-generated `_not-found` route only (no root layout exists by design — route groups own their layouts).
- **Phase 2 · Pass 2.1 — structure: hero Option A, video placeholder, product proof, nav + logo (July 2026, D17–D25):** photography-led hero rebuilt per the approved mockup — full-bleed F-7 stand-in (16:9 re-crop from the master, 1920w WebP 63 KB, `public/photos/hero-f7.webp`) under the teal duotone (`mix-blend-color`, tokenized) + a horizontally-symmetric scrim (RTL-safe); live 3-persona slider: labels render from `t.segments.cards[i].title` (verbatim-match with the Segments band by construction), slides/rails keyed by index (learning #1), 6s sage `railFill` doubling as the dwell timer (advance fires exactly at rail completion; 0.6s crossfade overlaps the next dwell), Ken Burns on the active slide, reduced motion = static slide 1 + plain manual tabs; serif accent per D18; sage/glass CTAs (D19); hero copy enters via **CSS animation, not Framer** — Chrome excludes the full-viewport photo from LCP as a background, making the copy the LCP element, and opacity-revealed content never emits LCP entries, so the h1/sub use a translate-only entrance that paints pre-hydration (observed 4G LCP vs Phase 1: EN 6.6s→2.1s, AR 5.9s→2.0s; note Lighthouse's default *simulated* mode overstates text-LCP behind the font/JS graph — compare in devtools-throttling mode). New `VideoTour` section (D20): poster overlay (F-9 + section scrim + sage play + 2:04 chip), accessible chapter `<button>`s (`aria-current`, Enter/click = seek+play), per-chapter 3px rails from one `timeupdate` listener (`scaleX`, `origin-left rtl:origin-right`, `motion-reduce:transition-none`), EN/AR caption stubs, `preload="none"`, never autoplays. New `ProductProof` section: the Phase 1 composite relocated verbatim (chips/lightbox/glow/parallax), entrances switched to `whileInView`, dashboard capture lazy. TrustBar compressed to a quiet hairline strip attached to product-proof. Full-width two-state nav + Log in link (D24/D25) with `[id] { scroll-margin-top }` so anchors clear the fixed bar. Real mark inlined via the `--color-logo-mark` token (D23) and swapped across favicon.ico (multi-res), `app/icon.tsx`/`apple-icon.tsx`, OG cards, and JSON-LD Organization logo (→ stable `public/brand/wazen-logo-512.png`); `lib/brand.ts` holds the path + fixed colorways for Satori contexts. `components/motion.ts` gains exactly `kenBurns`/`crossfade`/`railFill`; i18n gains `nav.login`, `hero.personasLabel`, `video.*`, `productProof.*` EN+AR. Verified: build/lint clean, grep gates pass, behavioral checks (remount EN↔AR on every new motion element, reduced-motion walkthrough, anchor offsets, slider/video sync, single hero preload) + screenshot matrix in `_preview/phase2.1/` (EN+AR × light+dark × desktop+mobile + nav states + favicon 16/32 renders), Lighthouse SEO 100/100 both locales. Owner-review fix-ups folded in before commit: **mobile persona tabs** redesigned to three equal-width rails in one row with a crossfading active-label line above (labels `sr-only` on the buttons — viewport-safe by construction); **nav drawer** closes on Escape and backdrop/outside click with focus returned to the menu button; **hybrid favicon** per the D23 sub-note.
- **Post-2.1 fix-ups #2 — owner preview review (July 2026):** hero persona rails re-specced to *exactly one non-empty rail at any moment* — the active rail's fill span mounts per activation (structural `cycle` key) and fills 0→1 over the dwell, outgoing rails empty instantly on unmount (no held fills, no reverse-fill), wrap leaves the others empty, mid-fill clicks restart from 0, identical on the mobile rail row, reduced-motion static states unchanged; the two ProductProof annotation chips removed (renders + EN/AR `hero.chips` dictionary entries); **every phone capture now opens the shared Lightbox** via `PhoneShot` (new `variant="phone"`: the capture stays inside the phone chrome at ~85vh; click/Enter open, backdrop/Escape/X close; trigger + close labels localized EN+AR — they were hardcoded English before) across Features PiPs, sync panel, ClientApp showcase, ProductProof and CTA phones (CTA's phone gained a real accessible name from the existing showcase strings); captures stay lazy.
- **Post-2.1 follow-up — the two queued defects cleared (July 2026):** OG/social card layout — EN headline 36px (two pre-broken lines, ≤3 rendered), chip fully visible with clear region spacing + `flexShrink: 0` guards, AR chip clears the phone frame, AR word order verified intact, Satori's Arabic advance-slack artifact documented in `lib/og.tsx`; WhyWazen mobile overflow — `min-w-0` on the proof grid column (learning #5 class). Verified: build clean, all four og/twitter cards render (`_preview/og-fix/`), no horizontal overflow at 360/390 EN+AR, WhyWazen unchanged at 1440, Lighthouse SEO 100/100 both locales.
- **Phase 2 · Pass 2.2a — Stage-2 redesigns: Problem + Features (July 2026, D26–D39 recorded):** Problem rebuilt as the chaos→calm split (D26/D27): the four oldWay items render as scattered statically-rotated `bg-surface/95` notes (rotation on an inner div — Framer owns the motion wrapper's transform) over F-2 at 20% under the shared duotone (now visible on mobile as a top band — the section's first mobile people), converging along a bold 3px sage `drawLine` connector (R2: full 120px column + chevron + dashed feeders; vertical variant on mobile) into ONE real capture fragment — the dashboard attention queue — in a sage-ringed frame above the four wazenWay check rows; sage radial glow (token-composed `bg-radial`) behind the calm half; ghost wordmark stays; zone labels = the old card headings as Badge eyebrows. Features tabs restyled per D21/D28/D29: pills → text-only progress-underline tabs (icons retired), 3px rail, the active rail's fill span keyed by a monotonic `cycle` (hero mechanic — exactly one non-empty rail, mid-fill clicks restart, outgoing rails empty by unmount), rail fill = the 6s auto-advance timer (`DWELL_MS`, D29), **in-view gate anchored to the tablist** (first `useInView` in the repo; a section-level threshold is unreachable on tall mobile layouts — and pausing once the reader scrolls into the panel is deliberate: never swap a panel under a reader), permanent stop on first interaction, keyboard/ARIA verbatim from the pill era; panel rebuilt as the composed scene (D28): end-offset teal-mist wash (token gradient), static ghost chapter digits 01–05 (Latin both locales, D12), browser capture overlapping the wash's start edge, breakout phone crossing the wash's bottom edge (lightbox preserved), two real-capture fragment chips per tab entering at the product register. New `components/ui/CaptureFragment.tsx`: aspect-locked next/image region crops (translate-percentage geometry, per-locale physical regions — AR captures are native-RTL re-captures, not mirrors); the ledger's compliance-row family proved illegible at chip scale (~11.5:1), so the roster chip uses the same capture's title+count region instead (§5 ledger family unchanged — same capture). **Owner-review amendments folded in before commit:** per-tab chip counts are asymmetric BY DESIGN — 2 (clients) / 2 (check-ins) / 1 (progress) / 1 (plans) / 0 (messaging) — a chip exists only where a real crop tells that tab's story: the progress tab runs Plan Adherence alone because every metric tile in its own panel capture is already visible in the 21/10 crop (any tile chip = duplication; the owner-offered body-fat/muscle-mass swaps fail the same test), the plans tab dropped the roster-count chip (roster stat on a templates story), and messaging runs chipless (the scene composes correctly at 0 chips — chips are absolutely positioned, no orphaned spacing). Problem's ghost wordmark moved from the shared centered GhostWordmark to a local bottom-start edge-clipped span (owner pick "Option C" from three injected-style renders — centered it sat half-occluded between the notes and the fragment, reading as an artifact; CTA keeps the shared centered treatment). The two Templates_Tab desktop captures carry owner-approved interim raster edits (see the re-capture ledger's DOCTORED entry). `components/motion.ts` gains exactly `photoReveal` + `drawLine`. `lib/i18n.ts`/`lib/screenshots.ts` untouched; anti-uniformity contract (§6) held: chips/ghost-numbers Features-only, scattered notes Problem-only, off-viewport bleed reserved for CTA. Verification matrix in `_preview/pass2.2a/`.
- **Phase 2 · Pass 2.2a.1 — owner corrections (July 2026, D40–D41):** Features tabs 2/3 panel captures swapped to the coach view-client pages (`coachViewCheckin`/`coachViewProgress`, new `lib/screenshots.ts` entries, dimensions verified per file with sharp metadata) — the tab copy tells coach stories and the old captures showed client views; chip re-check found no duplication against the new captures, pairings unchanged; two queued follow-ups recorded (tab-2 `primaryAlt` wording → 2.2c i18n pass; BrowserFrame URL mismatch → 2.3 re-capture ledger). Pricing default currency → USD on both locales (D41, partial supersession of D6): constant initial state, the per-locale re-default effect and touched-ref deleted, toggle/prices/strings untouched. Also calibrated here: the AR fragment regions for the analytics-family chips shipped in 2.2a started below the AR tile's stacked numeral (AR tiles put the number above the labels; the defect escaped review at full-page scale) — AR `y` 17→12.3 on all three entries, every AR chip re-verified per tab.
- **Phase 2 · Pass 2.2b — Stage-2 redesigns: HowItWorks + Segments + photo divider ① (July 2026, D42):** HowItWorks rebuilt as the zigzag timeline (D31): cards/icon bubbles/ghost numbers die; a vertical sage spine (2px, sage-dark→35% gradient) draws top→bottom via `drawLine` `hiddenY→visibleY` + `origin-top` (position wrapper static, Framer owns only the inner scaleY — center axis on desktop, start edge on mobile); numbered nodes (Latin digits, D12; `ring-bg` masks the spine) pop at the copy register, copy `fadeUp`, visuals settle at a local 0.7s product-register variant (the Problem `fragmentReveal` pattern); steps alternate sides via logical grid columns (`grid-cols-[1fr_96px_1fr]`, RTL mirrors free), mobile collapses to `[60px_1fr]` with the visual below (approved render). Three distinct visual objects: **01** frameless invite-modal fragment (`CaptureFragment` of `coachInvite`, per-locale regions — the centered dialog makes EN/AR genuinely coincide at `{x:33, y:31.9, w:34}`, aspect 3/2, −2°); **02** tilted plan phone (+3.5°, `PhoneShot clientMobilePlans` — the 9/19 frame window IS the D37 crop, lightbox kept; the mid-capture Binaa-footer defect remains per D37, queued for 2.3); **03** frameless adherence-trend strip (`coachAnalytics`, aspect 84/47, −1.5°; EN `{x:26.8, y:34.6, w:52.5}` start-anchored cropping mid-chart like the mock, AR `{x:15.4, y:28.8, w:58.1}` — the AR card is proportionally taller so its box is wider and end-anchored to keep title + full chart + date labels; the Features `weight`-fragment precedent). Tilts are static CSS `rotate-*` on inner wrappers (Tailwind v4 compiles these to the native `rotate`/`scale` properties — they can never fight Framer's `transform`). Segments rebuilt as the persona mosaic (D32 + R1): photo-top cards die; `md:grid-cols-[1.15fr_1fr]` mosaic (F-3 large `row-span-2` 584px; F-5/F-6 stacked 283px; mobile full-width stack 300/240px), copy ON the photo (verbatim `t.segments.cards[i]`, hero label match by construction) over shared duotone + **per-panel bottom-anchored horizontal-symmetric scrims**; panels `photoReveal`-stagger 0.12 (local container variant), hover = CSS `scale-[1.03]` on the image inside the fixed frame (`motion-safe:` — off under reduced motion) + shadow lift; photos decorative `alt=""` (D42). **R1 evidence (measured, not eyeballed):** Playwright harness hid the copy, sampled the max-luminance rendered pixel under each text element per panel, and computed ratios against each element's EFFECTIVE color (title pure white; body white@80% alpha-composited over the worst pixel) — worst cases across EN+AR × light+dark × 1440+390: F-3 title 5.21 / body 5.72 · F-5 title 5.43 / body 5.95 · F-6 title 5.23 / body 5.14 — all ≥ AA-normal 4.5:1 (strict bar; titles not argued as large text), light ≡ dark exactly (panels self-dark). F-6, not the predicted F-5, was the actual weak panel (bright wall behind the EN title tail, 4.01 → its scrim mid-stop raised to `/62 via-55%`). New `PhotoDivider.tsx` (D30): pure `<figure>` band (340/260px) between Features and HowItWorks — branded `PhotoPlaceholder` D-1 interim (sized `h-full w-full`; its baked-in rounded corners pushed outside via an oversized wrapper under the band's `overflow-hidden`) beneath the shared duotone + scrim, ONE overlay line reusing `t.features.tabs[1].caption` with the sage rail, `photoReveal` only, no section semantics, no new strings. `LandingPage.tsx`: divider inserted between Features and HowItWorks (only change). `motion.ts`/`lib/i18n.ts`/`lib/screenshots.ts` untouched; **owner device-pass verdict at plan approval: Features auto-advance trigger stays as built (req-12 margin change not taken — Features untouched)**. Anti-uniformity (§6) held: copy-on-photo Segments-only, one angled phone here, ghost numbers stay in Features; adjacency seams verified (Features composed scene → dark photo band → light timeline → photo mosaic — no shared skeleton/presentation/background). Verified: build+lint clean, grep gates pass, 16/16 behavioral checks (EN↔AR toggle remount both directions on every new motion element, reduced-motion walkthrough — static full-height spine, opacity-only, no hover scale — spine 0→1 draw sampled mid-flight, divider placement + caption identity), 29-shot matrix in `_preview/pass2.2b/` (3 sections × EN+AR × light+dark × desktop+mobile + 2 seam shots + 3 reduced-motion shots) visually compared against the five Stage-2 mocks.
- **Phase 2 · Pass 2.2c — Stage-2 redesigns: ClientApp + WhyWazen + CTA + the queued i18n changes (July 2026, D43):** ClientApp rebuilt as the page's mid-point cinematic dark beat (D33): `primary-dark→darker` gradient band (theme-invariant, the Problem/CTA precedent), D-2 backdrop slot as `PhotoPlaceholder` interim under the shared duotone at 55% + a deepening scrim (texture layer oversized so its centered label block falls below the section clip; the visible D-2 label sits as a separate corner instance at the mock's top-end position, desktop-only — at 390px it collided with the centered header; R3 note left at the site: reassess the two-white-phones-clinical concern only after real D-2, 2.3); three phones — check-in (−4°), home (center, raised), **white-label NEW-CAPTURE slot** (+4°, `PhotoPlaceholder` inside real `PhoneFrame` chrome, fictional coach unnamed; its hint span hidden below md via a container-scoped selector — it overflowed the 9/19 window at phone-row widths — the component itself untouched); captions: **the dictionary order didn't change, the ROW order did** — the check-in capture keeps "Weekly check-in" (`phones[1]`) and the home capture keeps "Today's plan" (`phones[0]`), each caption describing the capture it sits under; the third caption = the new `whiteLabelCaption` key (D34 Option A verbatim; the bold sage lead is a PRESENTATIONAL split on the first " — ", reassembled byte-exact, verified in both locales incl. the AR em-dash under RTL). **`phoneFloat` added to `motion.ts`** per the §3 table exactly (±4px translateY, 6s ease-in-out infinite alternate, 0.8s stagger via `custom`) — the page's only loop besides rail timers; float gated on `useReducedMotion` (MotionConfig alone strips the tween but still APPLIES the final keyframe as a static −4px — the explicit gate keeps reduced motion at zero transform, the D21 Features precedent). WhyWazen rebuilt as the editorial spread (D35): start column = in-component start-aligned header (SectionHeader's `align="left"` is physical and was out of scope) + numbered pillars 01/02/03 — **Fraunces italic sage on EN / Tajawal 800 sage on AR, keyed to the LIVE language state** (D18 hero mechanic, existing font instance; runtime-verified: pillar font flips on toggle, Fraunces never on AR) + bilingual badge; end column = layered collage (F-7 duotone panel → coachProfile `BrowserFrame` overlapping, lightbox kept → founder pull-quote card overlapping the frame's stat row deliberately, `border-inline-start` sage, quote serif-conditional like the pillars, NS avatar until F-11 → profile-caption annotation at bottom-start), all insets logical; collage layers enter back-to-front with stepped local-variant delays (photoReveal → 0.7s/+0.3 → 0.5s/+0.55), **delays zeroed via `useReducedMotion`** (MotionConfig strips transforms, not delays). CTA recomposed per the pre-seed (D36): white card + `MiniDashboard` skeleton die (**MiniDashboard was a local function inside CTA.tsx — no separate component file existed to delete**); backdrop unchanged (F-9 @12% + shared centered GhostWordmark); copy start-side with the four trust rows as a 2×2 grid — **keyed by index, fixing a latent learning-#1 bug** (they were keyed by translated string inside a `whileInView` parent since Pass A) — sage pill (D19) + restyled login line; composite end-side (coachDashboard top crop in a BrowserFrame, decorative `alt=""` — no dictionary key exists and the Req-10 list was closed — + check-in `PhoneShot` with lightbox + accessible name) bleeding off the END edge via negative `margin-inline-end`, clipped by the section (verified both directions, no page h-overflow; §6 bleed stays CTA-exclusive). **i18n: exactly the four Req-10 changes, both locales, diff-verified** — `whiteLabelCaption` added (D34 verbatim) · `cta.cardTitle`/`cardSub` retired · the three D42-orphaned `segments.cards[i].photoAlt` retired · `features.tabs[1].primaryAlt` fixed to the owner-approved wording (EN "A client's submitted check-ins as the coach reviews them in Wazen" · AR "متابعات العميل المُرسلة كما يراجعها المدرب في وازن"); `tabs[1].caption` (the PhotoDivider dependency) untouched; two further orphans found and deliberately deferred (D43). Next-16 image docs were read before the pass's `next/image` work (2.2b session; conventions unchanged: `fill`+`sizes`, lazy below-fold, `priority`→`preload`). Verified: build+lint+tsc clean, grep gates (color literals, retired keys zero hits repo-wide, i18n diff = exactly four, screenshots.ts diff empty, D8 phrasing, Fraunces conditionals), 21/21 behavioral checks (EN↔AR toggle remount both directions, phoneFloat running under normal / zero-transform under reduced, lightboxes on phones + profile frame, CTA clip LTR+RTL, live serif flip), 30-shot matrix in `_preview/pass2.2c/` (3 sections × EN+AR × light+dark × desktop+mobile + 3 seam shots + 3 reduced-motion shots) compared against the four Stage-2 mocks.
- **Phase 2 · Pass 2.4 follow-through (July 2026):** EN Terms capacity claim corrected "scalable to 100+" → "up to 100" with add-on bundles (owner-granted exception to the D16 Terms-stays-as-is rule; no AR equivalent exists — Terms/Privacy are EN-only routes); repo-wide "100+" grep zero across app/components/lib.
- **Phase 2 · Pass 2.4 — pricing truth alignment + tab metadata + hygiene; branch merge-ready (July 2026, D52):** Pricing copy aligned to Billing_Final.md EN+AR (D52): Premium bundle → "+5 clients from $9/mo · bundles up to +50" (AED 33, owner-set), all "scale to 100+" → "up to 100", "Basic branding customization" moved to Professional (Premium inherits via "Everything in Professional"; white-label footnote verbatim), **three not-yet-implemented rows removed** (Professional "Scheduled messages" · Premium "Advanced automation" + "Suggested follow-up list" — Phase-7 verdict two-of-five; the §6 reminder re-adds them when gating ships), **Premium rebalanced to 6 rows** with "Larger client bundles (+25 and +50)" + "Longest data export window" (retention truth gate fired at the 15/45/90 FAQ figures → owner ruling: in-app 15/45/90 verified, D5 stands, retention FAQ untouched, Billing_Final §3.2 = doc error owner corrects separately; the row shipped figure-free), FAQ Q8 rewritten to the final 6-row lists; yearly $490/$990 verified, AED pair untouched. Tab metadata (owner-ruled scope extension): `<title>` → "Wazen" / "وازن" (share titles unchanged), `app/icon.tsx` + `app/apple-icon.tsx` regenerated light-mode (light surface, teal mark), `app/favicon.ico` deleted (supersedes the D23 favicon sub-note), viewport theme-color pair verified non-forcing and kept. D43 closed: `clientApp.phones[2]` + `problem.photoAlt` retired EN+AR (zero-consumer greps). Hygiene: the three `hero_rb_*.jpg` reviewer sims deleted; the six white-label spare captures moved out of the served tree (`public/Wazen-Screenshots/spares/` → `_photo-originals/spares/`, untracked). Dead-code sweep run and REPORTED (findings retained: `scaleIn`/`railFill` variants, `LOGO_SAGE`/`LOGO_DARK`, `clientPlans` SHOTS entry, export-only-dead `LOCALES`/`Screenshot` — all in files out of this pass's touch scope; `PhotoPlaceholder.tsx`/`useCountUp.ts` structural-keep; `VideoTour`+`lib/video.ts` flag-gated launch configuration) — queued as a single post-merge cleanup commit candidate. Pricing.tsx untouched functionally (feature rows are pure i18n); its stale bundle-math comment corrected.
- **Phase 2 · Pass 2.3b — final integration, Phase 2 build closed (July 2026, D49–D51):** the ClientApp third phone swaps its NEW-CAPTURE placeholder for the real white-label demo capture (D49 — fictional "Apex Coaching" brand; `clientMobileWhiteLabel` entry with explicit 1170×2520 dims, the pass's one sanctioned `lib/screenshots.ts` change; lightbox + caption-as-alt per the phones-1/2 pattern; PhotoPlaceholder loses its last consumer, component kept). Edge-scrollbar check per the pass gate: geometrically NOT cropped by the 9/19 window (cover crops bottom-only), measured invisible at in-place scale and faint-band-only in the Lightbox (L≈224 vs 251) — owner-accepted, ledger-queued (D50). Hero slide-1 mobile crop finalized from three rendered candidates → `object-[75%_50%]` below `md`, desktop untouched (D51). Video section ships HIDDEN (`SHOW_VIDEO_SECTION = false`) with the full enable procedure documented in §6 ("Enabling the video section"). Re-capture ledger + F-11 owner-deferred post-launch (D50). Verified: build+lint clean, grep gates (i18n diff empty · screenshots diff = the one entry · flags diff = the one value), behavioral (third-phone lightbox opens the white-label capture EN+AR · no `<video>` in either locale's DOM · hero mobile renders the picked crop), shots in `_preview/pass2.3b/` (ClientApp matrix ×8, hero crop candidates ×6, white-label edge zooms + lightbox proofs, video-absence seam). Owner spare captures parked untracked in `public/Wazen-Screenshots/spares/` (6 files — desktop client-home ×2, coach-dashboard desktop ×2 + mobile ×2) for future use.
- **Phase 2 · Pass 2.3a — photo integration (July 2026, D46–D48):** the F-7 hero stand-in ×3 replaced with per-persona photos (slider order: fitness→hero-h1 · nutrition→hero-h2 · health→hero-h3), all environment scenes without people (D46, partial supersession of D22). **Pre-build render gate** (new process rule, first application): minimal wiring → 8 renders + lettering zoom → owner verdict BEFORE the full build; outcome — slides 2/3 approved as wired, slide 1's AI master rejected (featureless right wall read as a crop defect at 16:9; object-position provably can't fix it — a 4:5 master covering 16:9 always shows full width) and replaced by the owner with a 1920×1280 stock interior (same filename), verified clean at the gate's pixel-level checks (mirror reflections equipment-only, no brand marks legible; D46 photo notes). Slide 1 keeps `preload` — learning #6 re-verified in devtools-throttling mode (4× CPU, ~4Mbps): LCP element is the hero copy in EVERY run both locales (dev-mode timings noisy 1.3–4.0s, element identity stable); slides 2–3 lazy (verified: exactly 1 eager + 1 preload link + 2 lazy). Hero alt audit outcome: the slide photos are decorative (`alt=""` inside the `aria-hidden` layer, no dictionary key exists) — **zero i18n changes this pass**. Divider D-1 + ClientApp D-2 landed as owner-accepted v1s (D47) — `PhotoPlaceholder` lost those slots but keeps the white-label phone slot; R3 closed at `opacity-55` with the divider scrim strengthened for D-1's brightness (D48, measured contrast in-row). `hero-f7.webp` pruned (zero-consumer grep; WhyWazen keeps its own `f-7.webp`). **package-lock.json rider:** regenerated after the crash corrupted `node_modules` (`npm ci` refused the stale lockfile; `npm install` re-resolved, no `package.json` change; the corruption also broke ESLint mid-pass — fixed with a clean `npm ci` on the new lockfile). Dev-environment learning recorded: a same-filename asset swap is NOT picked up by the dev server's persisted Turbopack image cache — even across server restarts and with `Accept`-negotiated variants diverging; the fix is deleting `.next` (bit this pass twice as phantom-stale hero renders). Verified: build+lint clean post-`npm ci`, grep gates pass, behavioral suite (3-distinct-photo slider cycle EN+AR · single eager hero image · reduced-motion static slide 1 on h1) all pass, matrix + R3 triptych + reduced-motion shot in `_preview/pass2.3a/` (incl. the render-gate set).
- **Phase 2 · Pass 2.2c.1 — owner device-review fix-ups on 2.2c (July 2026, D44/D45):** proposal round first (no code: three WhyWazen mobile treatments + two CTA mobile corrections as injected-style renders in `_preview/pass2.2c1/`, after diagnosing the CTA read — the −96px mobile bleed ratio matched desktop's, so the accidental-crop read came from composite scale + the clip edge bisecting the greeting card, not the bleed value). Owner picks implemented: WhyWazen mobile = Option A pulled-up overlap stack (D45 — layering preserved in flow layout, stat-row overlap intact, lightbox reachable, `max-lg`-scoped so desktop is pixel-identical: 0-diff spot-check); CTA mobile = bleed **removed** below `lg` (D44, partial supersession of D36's mobile clause — owner decision superseding both proposal options; desktop bleed untouched, layout-identical spot-check: frame edges byte-positioned, residual diff confined to dev-server image-derivative resampling inside the bitmaps). Verified: build+lint clean, no horizontal overflow at 360/390 both locales (learning #5 class), shots in `_preview/pass2.2c.1/` (2 sections × EN+AR × light+dark × 390 + 360 CTA overflow checks + desktop spot-checks).
- **Pass C.2b — photo placement (closes Pass B):** the six sourced photos (F-2/3/5/6/7/9 — D15) cropped to their slots' aspect ratios with sharp and exported as optimized WebP into `public/photos/` (19–79 KB each; JPG originals + unused F-8/F-10 moved to untracked `_photo-originals/`). Every placed `PhotoPlaceholder` replaced with `next/image` (`fill` + `sizes`, lazy — all slots are below the fold, `scaleIn` motion, alt text localized EN+AR in `lib/i18n.ts`): Problem F-2 (3:5 portrait, desktop-only column), Segments F-3/F-5/F-6 (3:2 card headers), WhyWazen F-7 (4:5, anchored `object-[50%_30%]` below `lg` so the 3:2 mobile crop keeps the subject's head), CTA F-9 (full-bleed 12% backdrop; corner slot tag removed). Fixed the aspect-ratio/min-width mobile viewport bug found during verification (learning #5). `.gitignore`'s `_preview/` entry was encoding-mangled and inert — rewritten, plus `_photo-originals/`. `PhotoPlaceholder` has no consumers left; the component stays for the optional F-4/F-8 slots, and the "NS" initials avatar remains until F-11 exists. Verified EN+AR × light+dark × desktop+mobile (32 shots in `_preview/c2b-*`).

---

## 6 · Remaining work

### Pass B — owner asset production ✅ complete

**Screenshot captures ✅ shipped (C.2a) · photo set v1 ✅ sourced and shipped (C.2b, D15 — TEMPORARY, refine post-launch).** Only the founder portrait (F-11) and the optional F-4/F-8 slots remain unsourced — none blocks launch.

**Re-capture checklist** — ✅ completed; kept as the reference sheet for any future re-capture session:

- **Demo data:** 9–12 clients, realistic regional male+female client names are fine in UI lists (data, not photography); real-looking emails (karim.a@gmail.com — never `*.client@example.com`); compliance spread 55–100%, no 0% in visible rows; templates "Used by 3/5"; seeded 6–8-message coach↔client conversation (plan feedback, not lorem); healthy adherence trend (no collapse to zero); active nutrition/medication plans (no ended-plan empty states).
- **Chrome hygiene:** tour banners dismissed, no localhost/debug overlays, production-looking URL, professional account name.
- **Desktop captures:** ~1280–1440px viewport; per-feature story crops, not full pages.
- **Phone captures (D9):** Chrome DevTools device emulation 390×844 @ DPR 3 — authentic for a PWA. Set: client Today, Plans, Check-in submit, Progress chart, Messages. Same demo session as desktop.
- **Locales:** duplicate desktop + phone key sets in Arabic UI for /ar. **Seed order (D11): seed EN → EN shots → seed AR → AR shots — never mix locales in one seeded dataset.**
- **Environment:** `VITE_SHOW_DEV_TOOLS` unset before capturing; Docker bakes `seed.ts` — rebuild the image before seeding.
- **File naming (D13):** drop files into the delivered tree — `public/Wazen-Screenshots/{Coach|Client} Dashboard/{Desktop|Mobile}/{en|ar}/<Name>.png`; `lib/screenshots.ts` maps them.
- **Features tabs 2/3 interim captures (D40 + 2.2a.2 addendum):** tab 2 = `View_Client_Check_in` (top crop) · tab 3 = `View_Client_Progress_2` (BOTTOM-anchored crop — the visible region is the Adherence & Wellness Averages card). The 2.3 re-capture replaces both with purpose-shot coach-side Check-ins/Progress views (and resolves the BrowserFrame URL mismatch noted in D40).
- **Per-locale seed runs produce different stat values across locale captures** (e.g. plan adherence reads 81% in the EN Analytics capture, 79% in AR — D11 seeds run once per locale): acceptable now (fragment chips are decorative and no dictionary string claims the numbers); unified naturally by the 2.3 re-capture's seed runs.
- **Known capture defects — OWNER-ACCEPTED FOR LAUNCH, deferred to the post-launch re-capture session (D50, 2.3b).** Entries and history kept as that session's shopping list (all Lightbox-zoomable only; nothing reads at page scale). One 2.3b addition: `Client Dashboard/Mobile/{en|ar}/Whitelabel_Home_Tab.png` carries a faint edge scrollbar (physical right EN / left AR — invisible in the in-place phone, faint in the Lightbox; D49 note).
  - `Coach Dashboard/Desktop/{en|ar}/Templates_Tab.png` — **DOCTORED (owner-approved interim, 2.2a amendments; superseded by the 2.3 re-capture):** the original files showed "USED BY 0" / "يستخدمه 0" on all five template cards (named defect class from the original audit; checklist requires "Used by 3/5"), and EN additionally an undismissed "Templates workspace" tour banner. No alternative capture existed (audit: the only other coach plans-story captures contradict the tab's fixed alt text). Raster edits applied with sharp, exact pixel dimensions preserved (2560×1600 / 3840×2400 — `lib/screenshots.ts` frozen): EN = tour banner region filled with sampled page bg (247,250,252) + five badge digits "0"→"3" cloned from the "WORKOUT PLANS · 3" heading digit (donor patch flattened to the white card bg); AR = the same five digit swaps from the "خطط التمرين · 3" heading digit. Verified seamless at 4× zoom + Lightbox. The 2.3 re-capture replaces both files outright (seeded "Used by 3/5", banners dismissed).
  - `Coach Dashboard/Desktop/ar/Templates_Tab.png` — the in-app "Powered by Binaa Labs" page footer remains visible at the capture's bottom edge (zoom-only; NOT raster-edited — dimension lock forbids cropping); stays queued for the 2.3 re-capture.
  - `Client Dashboard/Mobile/{en|ar}/Plans_Tab.png` — the in-app "Powered by Binaa Labs" footer sits mid-screen (~61% height; short page content) and is visible in ANY phone-shaped crop (D37 premise correction). Re-capture with longer plan content or scroll state that pushes the footer off, or accept until native fix.

**Photography briefs** — kept as the reference sheet for the v2 refinement (D15). Global rules: no women anywhere in frame (D7), no visible third-party logos, muted/desaturated tones that let teal `#3D8C84` / sage `#7BA898` UI sit on top, no neon gym lighting. Free-license sources (Unsplash/Pexels licenses) or own shoots.

| Slot | Status | Brief |
|---|---|---|
| F-2 | ✅ v1 shipped | "The old way": man buried in admin — phone + laptop + papers, head in hand; moody/dark; must sit on dark teal `hsl(186,21%,14%)`. Alt composition: hands-only phone flooded with chat notifications. Portrait crop, Problem section side column. |
| F-3 | ✅ v1 shipped | Male coach + male client mid-session (spot, pads, form cue); authentic modern gym; horizontal 3:2; bright neutral light. Boxing/MMA reads regionally right. Segments card 1. |
| F-5 | ✅ v1 shipped (accepted D7 edge concern — D15) | Two men in advisory conversation at a table — tablet/notebook, warm non-corporate setting. Alt: flat-lay meal-prep + plan + male hands. 3:2, segments card 2. |
| F-6 | ✅ v1 shipped (bg poster/sticker noted — D15) | Male practitioner consulting male client, warm clinic/office (not hospital-sterile); supports the medication/supplement differentiator. 3:2, segments card 3. |
| F-7 / F-10 | ✅ F-7 shipped (owner's pick; F-10 held in `_photo-originals/`) | F-7: Gulf men's gym scene — coach+client or strong single subject, modern upscale gym, dignified; vertical-crop survivable. F-10: male runner on a Gulf-city corniche at dawn. Own capture likely beats stock for both. |
| F-9 | ✅ v1 shipped (pre-darkened — D15) | Calm minimal desk + laptop, dusk/moody, no people. Used at ~12% opacity under the dark CTA — texture, not subject. |
| H-1 | ✅ shipped 2.3a (v1: stock landscape, gate swap — D46) | Hero slide 1 (fitness): environment scene, no people (D46 pivot). Shipped: 1920×1280 dark gym interior; wall sign/shelf stickers illegible (verified); mirror reflections equipment-only. |
| H-2 | ✅ shipped 2.3a (AI master — D46) | Hero slide 2 (nutrition): meal-prep counter scene, no people, 1856×2304. |
| H-3 | ✅ shipped 2.3a (AI master — D46) | Hero slide 3 (health): practitioner desk scene, no people, 1856×2304. |
| D-1 | ✅ shipped 2.3a (v1, narrative mismatch accepted — D47) | Divider ① band. Brief: man resting between sets, phone in hand. Shipped v1: battle-ropes action, subject right-of-center, 1920×822. |
| D-2 | ✅ shipped 2.3a (v1, narrative mismatch accepted — D47) | ClientApp backdrop. Brief: man at home post-workout, phone in hand, morning light. Shipped v1: gym deadlift close-up (no face), 1920×1279, at opacity-55 under duotone+scrim (D48). |
| F-4 | optional — not sourced | Male gym-goer seated between sets, phone in hand — "submitting a check-in"; client-app showcase support. |
| F-8 | optional — not sourced (a candidate sits unused in `_photo-originals/`) | Man at kitchen counter or post-workout at home, morning light, phone in hand — "Sunday check-in" routine. |
| F-11 | **deferred post-launch (D50)** — the "NS" initials avatar ships | Founder portrait: Naser Shadid; casual dark/teal top; soft natural side light; plain warm background or blurred gym; chest-up; croppable to ~200px circle; light + slightly moody variants for both themes. Replaces the "NS" initials avatar in WhyWazen when it exists. |

### Enabling the video section (post-launch, when the real walkthrough lands)

The section is fully built and wired (D20) but ships **hidden** (2.3b). A session with zero context enables it like this:

1. **Drop the video file** at `public/video/tour.mp4` (this exact path — `lib/video.ts` → `VIDEO_SRC` points both locales at `/video/tour.mp4`; if the recording's UI language differs per locale, drop two files and point `VIDEO_SRC.en` / `VIDEO_SRC.ar` at them). Captions (optional but wired): `public/video/captions.en.vtt` + `public/video/captions.ar.vtt` (`VIDEO_CAPTIONS`).
2. **Edit `lib/video.ts`:**
   - `CHAPTERS` — the four `{ start }` values, in **seconds**, matching where the four chapters begin in the real video (index-coupled to `t.video.chapters` titles in `lib/i18n.ts` — order: overview · clients & plans · check-ins · progress). Currently `0 / 34 / 72 / 101` from the dummy clip.
   - `VIDEO_DURATION` — the real clip length in seconds (renders as the `m:ss` chip).
   - `VIDEO_POSTER` — keep `/photos/f-9.webp` unless a real poster frame is preferred.
3. **Edit `lib/flags.ts`:** `SHOW_VIDEO_SECTION = true` (one line).
4. **Verify** (`npm run dev`, http://localhost:3000 and `/ar`):
   - The section renders directly after the hero on BOTH locales; poster shows (F-9 under the section scrim, sage play button, duration chip in Latin digits both locales — D12).
   - Click play → video plays; click each of the four chapter buttons → playback **seeks to that chapter's timestamp** and the chapter's 3px rail fills as its span plays (one `timeupdate` listener drives all rails).
   - Keyboard: chapter buttons reachable by Tab, Enter seeks+plays (`aria-current` follows the active chapter).
   - Reduced motion (OS setting or devtools emulation): rails render as set-states without tween; the video NEVER autoplays (it never does regardless — `preload="none"`).
   - EN↔AR runtime toggle after the section revealed: everything stays visible (learning #1 class).
5. **Build + lint clean**, then commit the two file edits (+ the video assets) and push.

### Launch checklist (separate from passes)

- [x] Real app URL into `lib/links.ts` (both signup + login) — `app.wazen.fit`, July 2026
- [x] BrowserFrame address-bar text matches the real app domain (Hero, Features ×5 tabs, WhyWazen, OG card)
- [ ] Click-test every CTA returns 200 (production live since merge `09dfaee` — unblocked, not yet run)
- [x] Re-verify OG preview via WhatsApp (primary share channel for the audience) — **DONE (owner retest, July 2026): the full https URL previews correctly; the earlier no-card report was WhatsApp's bare-domain TLD-linkification behavior (`wazen.fit` typed without a scheme), not a server issue.** Diagnosis trail kept for reference: server side is healthy, both suspects refuted, no code change made. Measured on production: EN og:image 101,228 B · AR 86,497 B (WhatsApp's limit ~300 KB); cold TTFB 0.13 s EN / 0.69 s AR, warm 0.12–0.40 s — the OG/twitter routes are **statically prerendered at build** (`○ (Static)` in the build output), so there is no request-time Satori cold start at all; WhatsApp's own UA gets 200 + zero redirects on both pages and both images; og tags sit at byte ~3.9–4.1 K of the HTML with `og:image:width/height/alt` + `og:title/url/type` all present; `Content-Type: image/png` + `Content-Length` served. The conditional dynamic→static swap was therefore not triggered (the routes are already effectively static). Likely remaining cause: **WhatsApp's per-URL preview cache** holding an earlier empty result (it persists up to ~30 days) — retest by sharing with a fresh query string (e.g. `wazen.fit/?v=2`); if a fresh-string share STILL shows no card, the next diagnosis step is a different phone/WhatsApp build and checking whether the plain `wazen.fit` (no scheme) form resolves differently in-app
- [ ] Run the live URLs through Google's Rich Results test / validator.schema.org (FAQPage + SoftwareApplication) — open per owner, July 2026; offline JSON-LD validation done in the SEO pass (D16); the hosted validators need the production URL, live since merge `09dfaee`
- [x] Final logo SVG swapped into `components/ui/Logo.tsx` + favicon/app-icon (`app/icon.tsx`, `app/apple-icon.tsx`) + OG cards + JSON-LD Organization logo — Pass 2.1 (D23, incl. the owner-approved hybrid favicon: simplified W-stroke at 16px, full mark ≥32px; **the .ico half of that scheme was later superseded — D52 deleted `app/favicon.ico`, the generated light tile is the single source**)
- [x] OG composite card overlap — **fixed (post-2.1 follow-up):** EN headline at 36px renders as its two pre-broken lines (≤3 with any natural wrap) so the sage chip sits fully clear of the region line (`flexShrink: 0` guards both); AR chip clears the overlapping phone frame (wider AR column padding); AR word order verified intact within and across lines. Known cosmetic Satori artifact documented in `lib/og.tsx` (Arabic advances measured unshaped → trailing slack beside heavily-joined words) — real fix needs a Satori/next upgrade or an own OG pipeline
- [x] WhyWazen mobile viewport overflow — **fixed (post-2.1 follow-up):** `min-w-0` on the proof grid column stops the address-bar URL's min-content from inflating the track (learning #5 class); no horizontal overflow at 360/390 both locales, no visual change ≥390px (the frame's own overflow-hidden + trailing spacer absorb the squeeze)
- [x] support@wazen.fit forwarding configured (footer contact) — owner-verified, July 2026

### Post-launch backlog

**Phase-7 reminder (billing feature gating):** when feature gating ships in the app: (1) **re-add the three D52-removed feature rows, EN+AR** — Professional "Scheduled messages" (رسائل مجدولة) · Premium "Advanced automation" (أتمتة متقدّمة) + "Suggested follow-up list" (قائمة متابعات مقترحة) — and restore their FAQ Q8 mentions; (2) revisit the Starter "full platform / no limits" copy sitewide — `pricing.microFree` ("Free for 5 clients · No credit card"), Starter's "Full core platform" row, the hero badge ("full platform, no limits"), CTA trust row ("Full core platform"), and FAQ Q1 — the free tier's "full platform" claim must match what gating actually grants.


**Real walkthrough video** — enable per "Enabling the video section" above (flag + `lib/video.ts` timestamps); also wanted as a secondary CTA · **founder portrait F-11** (per the §5 brief) — replaces the "NS" initials avatar in WhyWazen (D50) · **re-capture ledger session** (D50) — every §6 known-capture-defect entry (Templates_Tab interim + AR footer · `clientMobilePlans` mid-screen footer · D40 view-client interims + BrowserFrame URL mismatch · per-locale seed stat drift · white-label edge scrollbar) is the shopping list; wants a purpose-built seeded environment · **photo set v2** — replace the temporary stock set (D15) with own-shoot / Gulf-authentic photography; revisit the hero H-series environment scenes if person-in-frame personas are wanted (D46 superseded the D22 generation plan); replace the D-1/D-2 narrative-mismatch v1s against their original briefs (D47); fix the F-5/F-6 noted concerns; source F-4/F-8 if wanted · **dead-code cleanup single-commit candidate** (Pass 2.4 sweep findings, reported not applied: `scaleIn`/`railFill` variants, `LOGO_SAGE`/`LOGO_DARK`, `clientPlans` SHOTS entry, export-only-dead `LOCALES`/`Screenshot`) · **Terms/Privacy are EN-only today** (`/terms`, `/privacy` in the `(en)` group; the `(ar)` group has no legal routes) — AR legal pages when translated legal copy exists · real pilot-coach testimonials (headshot + one metric each) in the reserved slots · review-platform badges · dark-mode app captures if the app ships a dark theme · Features tablist arrow keys are not RTL-mirrored (ArrowRight always advances) — kept as-is in Pass 2.2a by explicit instruction (preserve-semantics-exactly); recorded here so it isn't rediscovered as a bug. · Features `SyncConnector` hydration mismatch for reduced-motion visitors (found at 2.2b verification, pre-existing since Pass A.1): the server renders the full-motion SVG (no `strokeDasharray`, three dot circles) while a `prefers-reduced-motion` client renders the static dashed variant — React logs a hydration error and regenerates the tree (also re-triggering the page's inline-script warning). Cosmetic (self-heals on hydration, reduced-motion clients only); fix belongs to a Features-touching pass — render the SSR-safe static variant first, or gate the branch on a mounted flag.
